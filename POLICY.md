# 🛡️ VoltPur Engineering Policy (دستور المشروع)

> هذا الملف **مُلزِم** لأي تعديل على VoltPur — بشريًا أو بوكيل (agent).
> الغرض: منع تكرار ما حدث في أغسطس 2026، عندما **مسح كوميت واحد شغل تصليب أمني كامل**
> (مصادقة PAdmin، تحقق SHA-256، النسخ الاحتياطي، SECURITY.md) بحجة «تحديث».

---

## 1) القاعدة الذهبية: لا ادعاء بلا كود، ولا رقم بلا قياس

| ممنوع | بدلًا منه |
|---|---|
| كتابة ميزة في README/CHANGELOG قبل وجود كودها | اكتب الميزة أولًا، ثم وثّقها |
| رقم أداء بلا ملف قياس محفوظ في المستودع | أرقام `/voltpur benchmark` أو CI فقط |
| خيار في `voltpur.yml` بلا مستهلك | احذفه أو نفّذه |
| وسم موديول `ACTIVE` وهو بلا كود/معطّل | `PARTIAL` / `PLANNED` بصدق |

**Check قبل كل إصدار:** `grep -rn "TBD\|قريبًا\|soon" README.md docs/` = صفر نتائج في قسم «يعمل الآن».

---

## 2) السلوك الافتراضي الآمن (Safe by default)

أي ميزة تُعدّل ملفًا، أو تحذف بيانات، أو تغيّر قواعد اللعبة، أو تفتح منفذ شبكة — **يجب** أن تكون:

1. **مطفأة افتراضيًا** (`enabled: false`) مع تعليق يشرح ما تفعله بالضبط وما لا تفعله.
2. **قابلة للرجوع**: إما بـ `revert` تلقائي (مثال: DynamicOptimizer) أو نسخة احتياطية مكتوبة بجانب الملف (`*.bak-<timestamp>`).
3. **مسجَّلة**: كل تغيير يكتب في اللوج: القيمة قبل → بعد + مسار النسخة الاحتياطية + «يحتاج ريستارت».
4. **مقيسة**: يُذكر الأمر الذي يثبت الأثر (`/voltpur benchmark`، `/voltpur modules`).

**ممنوع منعًا مطلقًا** بدون تأكيد صريح ونسخة احتياطية:
- حذف `plugins/` أو `server.properties` أو `ops.json`/`whitelist.json`.
- حذف مجلد `backups/` أو `logs/`.
- استبدال `server.jar` بملف غير متحقق منه.

---

## 3) العمليات المدمِّرة: بوابة إلزامية (Blast Radius Gate)

أي عملية تُعدّل أكثر من ملف واحد أو تحذف شيئًا تمرّ بثلاث مراحل:

```
1) STAGE      ↓ اعرض الخطة: ماذا سيُحمَّل، ماذا سيُحذف (أسماء صريحة)، ماذا سيُحتفظ به، المساحة الحرة، البصمة (sha256)
2) VERIFY     ↓ تحقق من: البصمة المطابقة + سلامة الملف (zip/manifest) + مساحة كافية + نسخة احتياطية جاهزة
3) CONFIRM    ↓ الطباعة: «تم» لا تُطبع إلا بعد نجاح فعلي، وتُكتب بصمة في voltpur-installed.txt
```

التنفيذ المرجعي: `VoltPurUpdater` (`/vo up <n>` ثم `/vo up confirm`).

---

## 4) لا `catch` فارغة

```java
try { work(); } catch (Exception ignored) {}        // ❌ ممنوع
VoltPurGuard.run("ModuleName", this::work);          // ✅ الصحيح
```
`VoltPurGuard` يسجّل عدد التشغيلات والاستثناءات ويحدّ من تكرار اللوج (مرة كل 60 ثانية لكل موديول)، فتظهر في `/voltpur modules`.
السبب: الموديولات كانت «ACTIVE» في الواجهة وهي ميّتة من ساعات لأن الفشل كان صامتًا.

---

## 5) حماية التراجعات (Anti-regression)

أي كوميت يلمس الكود يجب أن يحافظ على هذه الضمانات. **إن سقط منها بند، البناء يُعتبر فاشلًا**:

| # | الضمان | كيف نتحقق |
|---|---|---|
| G1 | PAdmin لا يعمل إلا على loopback + Basic Auth + باسورد إجباري | `VoltPurConfig.padmin*` + `InetAddress.getLoopbackAddress()` موجودان في `PAdminCommand` |
| G2 | المُحدِّث يرفض ملفًا بلا SHA-256 مطابق | `MessageDigest` + مقارنة الهاش في `VoltPurUpdater` |
| G3 | لا مسح شامل افتراضي | `update.clean-reinstall` default = false |
| G4 | لا كتابة على `server.properties` من فحوص الاستقرار | لا وجود لـ `ensureServerProperties` |
| G5 | no-op catch ممنوعة في كود الموديولات | `grep -c "catch (.*) {}"` = 0 |
| G6 | أسرار لا تُرفع | `.gitignore` يستثني `voltpur.yml` و`*.backup*` |

> **درس مستفاد:** ملف `SECURITY.md` نفسه (الذي كان يوثّق هذه الضمانات) هو ما مُسح. لذلك الضمانات هنا **مكتوبة مع طريقة تحققها**، وليست كلامًا.

---

## 6) سير العمل

1. **فرع لكل تغيير**: `fix/<scope>` أو `feat/<scope>`.
2. **وصف الكوميت** يجيب على ثلاثة أسئلة: ماذا تغيّر؟ لماذا؟ كيف أتحقق؟
3. **لا خلط**: لا تجمع «إصلاح أمني» مع «تجميل رسائل» في كوميت واحد كبير — هذا بالضبط ما شوّش مسار المشروع في أغسطس.
4. **قبل الدمج**: تشغيل `tools/verify/run-verify.sh` (يجب أن ينتهي بـ `RESULT: PASS`) + تسجيل في `docs/CHANGELOG.md` + تحديث أي رقم مذكور في README (والأفضل: لا تكتب أرقامًا يدويًا).
5. **قبل الإصدار**: تشغيل smoke test (إقلاع نظيف + `/voltpur modules` + `/voltpur benchmark`) والتأكد من عدم وجود استثناءات.

---

## 7) تعريف «منجَز»

الميزة «منجَزة» إذا وفقط إذا:
- [ ] لها كود يُنفَّذ فعليًا (وليس إعدادًا مكتوبًا)،
- [ ] يمرّ `tools/verify/run-verify.sh` بعد التعديل،
- [ ] تظهر في `/voltpur modules` مع `runs` غير صفري،
- [ ] لها قياس أو أثر يمكن التحقق منه بأمر واحد،
- [ ] موثّقة بحالتها الصادقة (يعمل / جزئي / مخطط)،
- [ ] آمنة افتراضيًا وقابلة للرجوع.

أي بند لا يحقق ذلك يُحذف من التوثيق، **أو** يُوسم `PLANNED` بصراحة.
