# VoltPur — خطة الوصول إلى "سوفتوير كامل" (Full Software Plan)

> مرجع: تحليل الكنسول (بناء `687683c` ثم `3e66189`) + مراجعة الكود المصدري.
> الهدف: تحويل VoltPur من "طبقة إدارة فوق Purpur + branding" إلى منتج يفعل ما يدّعيه فعلاً، بصدق قابل للإثبات.
> الحالة: خطة معتمدة للتنفيذ الكامل بموافقة واحدة (بدون طلب موافقة على كل تعديل).

---

## القاعدة الصادقة (يجب أن تظل في ذهننا)
- المحرك الحقيقي (شبكة، chunks، أداء، استقرار) = **Purpur/Paper/Pufferfish**. VoltPur يضيف طبقة أدوات/إدارة.
- المبدأ الحاكم لكل التعديلات: **لا نطبع رسالة "تم" إلا إذا حدث الشيء فعلاً وأمكن إثباته**. نحذف كل ادعاء لا يقابله تأثير حقيقي.

---

## المشكلة 1 — مجلدات العوالم لا تظهر (nether/end) وكأنها غير موجودة

### التشخيص الدقيق (من الكود + الكنسول)
- في `VoltPurWorldCheck.java` دالة `ensureWorldFolders()` تُنشئ **مجلدات فارغة** `world`, `world_nether`, `world_the_end` عبر `mkdirs()` بعد ~10 ثوانٍ من الإقلاع، وتطبع `Created world folder: world_nether/`.
  - هذه الرسالة **مضلِّلة**: تُنشئ مجلدًا فارغًا وليس عالمًا. والأسوأ: إنشاء مجلدات علوية فارغة قد يتعارض مع طريقة Paper الفعلية في تخزين الأبعاد.
- الكنسول يُظهر `[WorldFolderMigration] World storage migration is required` + `[LegacyCraftBukkitWorldMigration]` — أي أن **Paper نفسه يهاجر بنية تخزين العوالم** (سلوك طبيعي في إصدارات MC الحديثة). النذر/الإند يتم استيرادهما فعليًا (`world_nether`, `world_the_end`) وتُحمَّل 3 عوالم بنجاح.
- الاحتمال الجذري لعدم ظهور بيانات النذر/الإند: منطق "المسح الكامل" (`cleanReinstallKeepingWorlds` / `isWorldFolder`) عند `/vo up` قد يحذف `world_nether`/`world_the_end` إذا كانت (لحظة المسح) فارغة/بلا `level.dat` علوي، ثم يُعاد إنشاؤها فارغة → لا تُحفظ بيانات فعلية.

### خطة الإصلاح
1. **حذف `ensureWorldFolders()` نهائيًا** والرسالة "Created world folder" — نترك Minecraft/Paper يُنشئ ويدير مجلدات العوالم طبيعيًا (هذا هو السلوك الصحيح لأي سوفتوير سيرفر).
2. **تقوية كشف العوالم في المسح الكامل** (`isWorldFolder`):
   - اعتبار المجلد عالمًا إذا كان اسمه ضمن قائمة العوالم المعروفة (`world`, `world_nether`, `world_the_end`) **أو** يحتوي `level.dat` **أو** يحتوي مجلد أبعاد (`DIM-1`, `DIM1`, `region`, `dimensions/`) — بحثًا متداخلًا.
   - عدم حذف أي مجلد مرتبط بعالم مُعرّف في `bukkit.yml`/`server.properties` (`level-name`).
3. **سجل شفّاف للمسح**: طباعة المسارات المطلقة لكل عنصر (KEPT/WIPED) بدل عدّاد مبهم "removed 22 item(s)".
4. **توثيق سلوك WorldFolderMigration** في الرسائل: طمأنة المستخدم أن مهاجرة Paper طبيعية وليست خطأ.
5. **تحقق فعلي**: بعد الإقلاع، بدل "Created folder"، نطبع مسار كل عالم مُحمَّل + هل يحتوي بيانات region محفوظة (عدد ملفات `.mca`).

### إثبات الإنجاز
- مجلدات `world`, `world_nether`, `world_the_end` تظهر في متصفح ملفات اللوحة بعد أول تشغيل، وتبقى بعد `/vo up` مع بياناتها (ملفات `.mca` > 0 بعد زيارة الأبعاد).

---

## المشكلة 2 — لا يمكن تأكيد الإصدار الذي انتقلت إليه (+ بَق في المُحدِّث)

### التشخيص الدقيق (بَق مؤكَّد)
- في `VoltPurCommand.doUpdate()` مسار **بلا توكن** (وهو وضعك الحالي):
  ```java
  String releaseUrl = ".../releases/latest/download/VoltPur-26.2.jar";
  ```
  ⇒ ينزّل **دائمًا آخر إصدار** بغضّ النظر عن الرقم المختار. `/vo up 1/2/3...` كلها تثبّت نفس الأحدث.
- لهذا "تحسّ أن كل إصدار وإصدار مش عارف" — لأنك فعليًا تثبّت نفس الملف كل مرة.
- لا يوجد أي **بصمة إصدار** تُكتب أو تُعرض بعد التحديث. الدليل الوحيد الحالي هو هاش git في بانر الإقلاع (`@687683c` → `@3e66189`) — وأنت لا تعرف أن تنظر إليه.
- كل بناء يُنشئ Release بوسم `build-<run_number>-<sha>` (من `build.yml`) — إذًا يمكن تنزيل بناء محدد بدقة.

### خطة الإصلاح
1. **تنزيل البناء المختار بالضبط** (بلا توكن):
   - من `runId` المختار، نستدعي `GET /actions/runs/<runId>` لجلب `head_sha` + `run_number`.
   - نبني وسم الإصدار `build-<run_number>-<sha>` وننزّل أصله:
     `.../releases/download/build-<run_number>-<sha>/VoltPur-26.2.jar`.
   - fallback إلى `latest` فقط إذا فشل الوسم (مع طباعة تحذير صريح).
2. **بصمة إصدار (version stamp)**:
   - وقت البناء يُكتب في المانيفست/ملف `version.json` داخل الـ jar (run_number, sha, build_date).
   - عند `/vo up`، بعد التنزيل نقرأ بصمة الـ jar الجديد ونطبع: `Installed build #<n> (run <id>, commit <sha>, built <date>)`.
   - نكتب `voltpur-installed.txt` في جذر السيرفر بهذه البيانات.
3. **تأكيد بعد الإقلاع**: بانر VoltPur يطبع بوضوح `BUILD <run_number> | commit <sha> | built <date>` ويقارنه بـ `voltpur-installed.txt` ويطبع `✅ Update applied` أو `⚠ Mismatch`.
4. **`/vo version` و `/voltpur status`**: يعرضان البناء الجاري + آخر بناء ثُبِّت.
5. إصلاح تحليل `runId` في مسار التوكن أيضًا (اتساق).

### إثبات الإنجاز
- `/vo up 3` يثبّت البناء رقم 3 تحديدًا (هاش مختلف عن الأحدث)، والبانر بعد الإقلاع يؤكد رقم البناء والهاش المطابقين للاختيار.

---

## المشكلة 3 — مجلد plugin-pro (السبب الأصلي لبناء المشروع)

### التشخيص الدقيق (تضارب في الكود)
- **يوجد patch حقيقي** `PluginInitializerManager.java.patch` يسجّل `plugin-pro/` كـ `DirectoryProviderSource` **قبل** مجلد `plugins/` الافتراضي — وبناء 63 طبّق الـ patches بنجاح، أي أنه **مُطبَّق فعليًا**.
- **لكن** يوجد تضارب: `PAdminCommand` يكتب "plugin-pro/: organisational (Phase 1) — Paper loads from plugins/"، وبعض الوثائق تقول إن الـ patch أُلغي. ⇒ رسائل متناقضة تربك المستخدم.
- **الحقيقة التقنية الأهم (يجب أن تفهمها):** بلوجنات Bukkit/Paper **ليست مثل Fabric mods**. حتى لو حُمِّل الـ jar من `plugin-pro/` أولًا، فهو يُحمَّل في **مرحلة تحميل البلوجنات العادية** (بعد إقلاع نواة اللعبة)، ولا يستطيع تعديل محرك اللعبة **قبل** تشغيلها. "التحميل قبل اللعبة مثل Fabric" يتطلب أحد:
  - **Mixins** عبر mixin-loader/agent، أو
  - **Server patches** (paperweight) مُدمجة في الـ jar (وهذا ما نفعله نحن بالفعل للتعديلات الأساسية)، أو
  - **Java agent** (`-javaagent`).
- إذًا "أولوية التحميل" التي يمنحها plugin-pro = اكتشاف أبكر فقط؛ ترتيب التفعيل الفعلي تحكمه شجرة الاعتماديات (dependencies) لا ترتيب التسجيل.

### خطة الإصلاح (على مرحلتين)
**المرحلة أ — توضيح وتفعيل ما هو موجود بصدق:**
1. توحيد الرسائل: إزالة التناقض. الرسالة الصادقة = `plugin-pro/ = priority plugin folder (scanned before plugins/, standard Paper plugins)`.
2. سجل تحقّق فعلي عند الإقلاع: طباعة قائمة ملفات `.jar` الموجودة في `plugin-pro/` وتأكيد تحميل كلٍّ منها (اسم + نجاح/فشل)، ليرى المستخدم أنها اشتغلت.
3. اختبار بـ plugin تجريبي صغير يطبع رسالة، لإثبات التحميل من `plugin-pro/`.

**المرحلة ب — تحقيق طموح "مثل Fabric" (ميزة كبيرة، اختيارية):**
4. تصميم **VoltPur Mixin/Agent Loader**: مُحمِّل يقرأ `plugin-pro/` بحثًا عن وحدات mixin (أو `-javaagent`) ويطبّقها **قبل** إقلاع نواة الخادم، بحيث يمكن لوحدة أداء تعديل اللعبة فعليًا قبل تشغيلها.
5. توثيق واضح للفرق بين "priority plugin" و"pre-game mod"، وواجهة موحّدة لكليهما.

### إثبات الإنجاز
- أ: jar في `plugin-pro/` يظهر في سجل الإقلاع "loaded from plugin-pro" ويعمل.
- ب: وحدة mixin تجريبية تعدّل سلوكًا في اللعبة يظهر أثره قبل/أثناء الإقلاع.

---

## المشكلة 4 — التحسين المدمج: هل يحسّن الأداء فعلاً أم مجرد رسالة؟

### التشخيص الدقيق (الرسالة أكبر من الفعل)
- `VoltPurOptimizer.apply()` يكتب قيمًا في **`spigot.yml`** وقت التشغيل (مثل `ticks-per.hopper-check`).
- **المشكلة القاتلة:** `spigot.yml` يُقرأ **عند الإقلاع فقط**؛ الكتابة أثناء التشغيل **لا تُطبَّق هذه الجلسة**. وعلى سيرفر فارغ (hoppers=0) يحسب `hopper-check=2` (≈الافتراضي) ⇒ **تغيير صفري فعليًا**.
- لذا `Applied adaptive tuning: hopper-check=2` **رسالة تجميلية** في الغالب. الأداء الحقيقي 100% من Purpur/Paper.
- `/voltpur benchmark` يقيس أداء **الخادم ككل** (أي Purpur) لا مساهمة VoltPur — لا يوجد إثبات A/B أن محرّك VoltPur حسّن شيئًا.

### خطة الإصلاح (تحويله إلى تحسين حقيقي قابل للإثبات)
1. **تطبيق فوري فعّال عبر API وقت التشغيل** بدل الكتابة في ملف مؤجَّل:
   - حدود سبان الوحوش لكل عالم: `World#setSpawnLimit(...)` (يُطبَّق فورًا).
   - `view-distance`/`simulation-distance` ديناميكيًا تحت ضغط TPS: `World#setViewDistance/ setSimulationDistance` (API حديث، يُطبَّق فورًا).
   - كتابة `spigot.yml`/`paper` تبقى فقط للقيم التي تحتاج restart، وتُكتب في **الوقت الصحيح** (قبل تحميل العوالم) لا بعد الإقلاع.
2. **حلقة تكيّفية حقيقية**: مراقبة TPS/MSPT كل N ثانية، وعند الهبوط نخفّض الحدود ديناميكيًا ونرفعها عند التعافي — مع طباعة القيمة **المطبَّقة فعليًا** والقيمة السابقة.
3. **إثبات A/B**: أمر `/voltpur benchmark before|after` يسجّل MSPT/TPS/الذاكرة قبل وبعد تطبيق التحسين ويطبع الفرق الحقيقي، ويحفظه في `logs/voltpur-benchmark.txt`.
4. **صدق التسمية**: أي شيء لا يُطبَّق فعليًا يُنقل إلى PLANNED؛ لا رسالة "Applied" إلا بعد تأكيد التطبيق عبر قراءة القيمة الحية من الـ API.

### إثبات الإنجاز
- عند محاكاة ضغط (وحوش/entities)، تنخفض حدود السبان الحية فعليًا (تُقرأ من API) ويتحسّن MSPT في تقرير before/after.

---

## مشاكل إضافية رُصدت في الكنسول (تُعالَج ضمن الخطة)
- **`/restart` يفشل**: `Startup script './start.sh' does not exist! Stopping server.` — على Minestrator الحاوية تعيد التشغيل تلقائيًا فيعمل، لكن يُفضّل: رسالة أوضح أو دعم إعادة تشغيل متوافقة مع اللوحة بدل الاعتماد على script غير موجود.
- **`/vo up list` مكرّر ×2 في اللوج**: سببه كتابة الأمر مرتين متتاليتين (تنفيذان متداخلان)، والقائمة النظيفة اللاحقة أظهرت 10 نتائج فريدة صحيحة ⇒ **ليست بَق**. نضيف حماية "تجاهل الأمر إن كان تنفيذ سابق قيد العمل".
- **`SIMD/Vec: not loaded`**: إضافة `--add-modules=jdk.incubator.vector` لأمر تشغيل Java (توثيق + كشف تلقائي وتنبيه).
- **NPE أثناء الإقلاع**: تم إصلاحه في بناء 63 (Paper #13580) ✅.
- **عدّاد الوحدات 12/25**: تنظيف الوحدات الوهمية (PLANNED التي لا كود لها) أو نقلها لقسم "Roadmap" منفصل بدل حشوها في العدّاد.

---

## ترتيب التنفيذ (Phases)
1. **Phase 1 — إصلاحات صدق + بَقات مؤكّدة (أولوية قصوى):**
   - إصلاح بَق المُحدِّث (تنزيل البناء المختار) + بصمة الإصدار وتأكيد الإقلاع. (المشكلة 2)
   - إزالة `ensureWorldFolders()` المضلِّلة + تقوية كشف العوالم في المسح. (المشكلة 1)
   - توحيد رسائل plugin-pro + سجل تحقّق التحميل. (المشكلة 3-أ)
   - جعل التحسين يُطبَّق عبر API فعليًا + تقرير before/after. (المشكلة 4)
   - تنظيف عدّاد الوحدات + تنبيه SIMD + معالجة /restart.
2. **Phase 2 — ميزات حقيقية أعمق:**
   - Mixin/Agent loader لـ plugin-pro (طموح Fabric). (المشكلة 3-ب)
   - تحويل 2–3 وحدات PLANNED إلى تحسينات حقيقية (Hopper/Redstone) عبر server patches.
3. **Phase 3 — إثبات وقياس:**
   - لوحة benchmark A/B دائمة + توثيق أرقام حقيقية في `docs/BENCHMARKS.md` (بلا مبالغة).

## طريقة العمل والإثبات لكل تغيير
- كل تعديل: كود → commit → push → متابعة بناء GitHub حتى النجاح → إصدار جديد.
- لكل مشكلة: معيار "إثبات الإنجاز" أعلاه يجب أن يتحقق في الكنسول/الملفات قبل اعتبارها مكتملة.
