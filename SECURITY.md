# 🔒 سياسة الأمان — VoltPur

آخر تحديث: **2026-09-17** (بعد جلسة التصليب `fix/phase-0-1-hardening`)

## الإبلاغ عن ثغرة
**لا تنشر الثغرة علنًا.** أبلغ عبر صفحة [Security Advisories](https://github.com/tkjij77-ctrl/VoltPur/security/advisories/new) (مفضّل)، أو Issue بعنوان `[SECURITY]` **دون تفاصيل حساسة في العنوان**.

### ما يجب تضمينه
1. وصف موجز + التأثير.
2. خطوات إعادة الإنتاج (أقل تفاصيل ممكنة).
3. الفرع/الإصدار المتأثر.
4. اقتراح إصلاح (اختياري).

## الإصدارات المدعومة

| الفرع | الحالة |
|---|---|
| `ver/26.2` | ✅ مدعوم |
| فروع أقدم | ❌ غير مدعومة |

**زمن الاستجابة:** تأكيد الاستلام خلال 48 ساعة · تقييم/خطة داخل 7 أيام · الإصلاح حسب الخطورة.

---

## الوضع الأمني الحالي (صادق، وبدليل ملف/سطر)

### ✅ الضمانات المطبَّقة الآن
| # | الضمان | مكان التنفيذ |
|---|---|---|
| 1 | **PAdmin**: مطفأ افتراضيًا + يستمع على **loopback فقط** + **Basic Auth** إجباري (باسورد غير قابل للتفريغ) + مقارنة ثابتة الزمن + GET فقط + حد 2 خيوط + **قراءة-فقط** | `command/PAdminCommand.java` |
| 2 | **لا قراءة لـ Bukkit API من thread الـ HTTP**: لقطة (snapshot) تُبنى على المين ثريد كل ثانية | `PAdminCommand.Snapshot` |
| 3 | **المُحدِّث يتحقق من SHA-256** المنشور لكل بناء، ويرفض أي ملف غير مطابق أو غير صالح (zip/manifest/versions.list) | `command/VoltPurUpdater.java` |
| 4 | **تحديث على مرحلتين**: `/vo up <n>` يُحمّل ويتحقق ويعرض خطة، و`/vo up confirm` هو ما ينفّذ | `VoltPurUpdater.prepare/confirm` |
| 5 | **لا مسح شامل**: `update.clean-reinstall=false` افتراضيًا (يحذف `libraries/ versions/ cache/` فقط وعند الطلب الصريح) + نسخة `server.jar.bak-*` + `/vo rollback` | `VoltPurUpdater` |
| 6 | **`/voltpur optimize` يرفض العمل** إلا إذا كان `modules.hardware.auto-tune=true` صريحًا، ويأخذ نسخة قبل أي كتابة | `VoltPurTuning` |
| 7 | **لا كتابة تلقائية على `server.properties`** في فحوص الاستقرار (الدالة القديمة التي كانت تكتب `online-mode=false` أُزيلت) | `VoltPurWorldCheck` |
| 8 | **تنزيل البلجنات**: رفض غير http/https، منع `userinfo`/`fragment`/`..`، منع loopback/private/link-local/multicast + **IPv6 ULA `fc00::/7`** + **CGNAT `100.64/10`**، حد 100 MiB متدفق، متابعة redirects محدودة، فحص أنه JAR فيه `plugin.yml`/`paper-plugin.yml`، كتابة atomic، تسمية ملف آمنة | `command/PluginJarInstaller.java` |
| 9 | **Webhook ديسكورد**: HTTPS + نطاق `discord.com`/`discordapp.com` فقط | `VoltPurDiscord.isAllowedWebhook` |
| 10 | **Resource pack**: لا يُقدَّم إلا على مسار عشوائي `pack-<token>.zip` + بث للملف (لا تحميل كامل في الذاكرة) | `VoltPurResourcePack` |
| 11 | **الأسرار**: دعم `${ENV_VAR}` للتوكن والباسورد، و`voltpur.yml` + `*.bak-*` مستثناة في `.gitignore` | `VoltPurConfig.resolveSecret`, `.gitignore` |
| 12 | **كل موديول مسؤول عن إعلان فشله**: لا كتلة `catch` فارغة في كود الموديولات؛ الفشل يُحصى في `VoltPurGuard` ويظهر في `/voltpur modules` | `VoltPurGuard` |

### ⚠️ مخاطر متبقية (معلومة، غير مخفية)
1. **TOCTOU في تنزيل البلجنات:** فحص العنوان يتم بالـ DNS قبل الاتصال؛ خادم DNS خبيث نظريًا قد يعطي ردًا مختلفًا في الاستعلام الثاني. الجرعة المخففة: إعادة الفحص عند كل redirect. الحل الجذري (الربط بالـ IP المُتحقق منه + SNI) يحتاج مُنفِّذ HTTP مخصص.
2. **جودة البلجنات:** `/vo in` يتحقق من الشكل فقط. لا يوجد توقيع/هاش لروابط طرف ثالث، والبلجن يعمل بصلاحيات كاملة.
3. **`plugin-pro/`:** يجعل الـ JARs تُكتشف قبل `plugins/`، لكن **ترتيب التحميل النهائي تابع لاعتماديات البلجن** (`depend`/`loadbefore` أو `load: BEFORE|AFTER`) — لم نعُد ندّعي غير ذلك.
4. **`/vo up` يستبدل الجار المُشغَّل:** نكشف مسار الجار الفعلي (وليس `server.jar` بالضرورة) ونحتفظ بنسخة، لكن الاستبدال يحتاج ريستارت من اللوحة.

### 🧯 ما تم إصلاحه في هذه الجلسة (لأنه كان يُنافي السياسة)
- PAdmin كان **بلا مصادقة** على **كل الواجهات** (بعد أن كان محميًا ثم أُلغي في `f718a8c`).
- المُحدِّث كان **بلا تحقق SHA-256**، والفحص الوحيد «حجم > 1MB».
- `/vo up` كان **يمسح كل ملفات السيرفر** (plugins/configs/ops/whitelist/backups) **بلا نسخة احتياطية**.
- `WorldCheck` كان يكتب `server.properties` بما فيه `online-mode=false`.
- `ItemLimiter` كان يحذف غرض اللاعبين **افتراضيًا** وبدون تحذير.
- `DynamicOptimizer` كان يغيّر السبون/محاكاة المسافة **نهائيًا** ورغم ذلك مفعّل افتراضيًا.
- 30 كتلة `catch` فارغة كانت تُخفي فشل الموديولات.

> القاعدة الحاكمة الآن موثّقة في [`POLICY.md`](POLICY.md): **لا ادعاء بلا كود، ولا رقم بلا قياس، ولا عملية مدمِّرة بلا بوابة تأكيد ونسخة احتياطية.**
