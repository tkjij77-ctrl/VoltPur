# 🔒 سياسة الأمان — VoltPur

آخر تحديث: 2026-08-17

## الإبلاغ عن ثغرة أمنية

إذا اكتشفت ثغرة أمنية في VoltPur، **لا تنشرها علنًا**. أبلغ عنها عبر:

- **GitHub Security Advisory:** افتح issue خاصًا عبر صفحة [Security](https://github.com/tkjij77-ctrl/VoltPur/security/advisories/new) (مفضّل).
- أو راسلنا عبر Issues مع عنوان يبدأ بـ `[SECURITY]` دون تفاصيل حساسة في العنوان.

### ما يجب تضمينه في التقرير
1. وصف موجز للثغرة وتأثيرها.
2. خطوات إعادة الإنتاج (بأقل تفاصيل ممكنة).
3. إصدار VoltPur/فرع المتأثر.
4. (اختياري) اقتراح إصلاح.

## الإصدارات المدعومة

| الفرع | الحالة |
|--------|--------|
| `ver/26.2` | ✅ مدعوم (النسخ الحالية) |
| فروع أقدم | ❌ غير مدعومة |

## زمن الاستجابة

- **تأكيد الاستلام:** خلال 48 ساعة.
- **تقييم/خطة إصلاح:** خلال 7 أيام.
- **إصدار الإصلاح:** حسب الخطورة (الحرجة بأسرع وقت).

## ممارسات الأمان المطبّقة

- **لا أسرار نصية:** يدعم قراءة الأسرار (tokens/passwords) من متغيرات البيئة عبر صيغة `${VAR}`، و `voltpur.yml` مستثنى من git.
- **PAdmin WebUI:** معطّل افتراضيًا، مربوط بـ 127.0.0.1 فقط، ويتطلب مصادقة.
- **Auto-updater:** يتحقق من SHA-256 قبل استبدال `server.jar`.
- **تعديل الإعدادات:** opt-in مع نسخة احتياطية قبل أي تعديل.
- **المواد الحساسة:** لا نجمع بيانات لاعبين، ولا نرسل بيانات لأطراف خارجية.
