--- a/src/main/java/io/papermc/paper/plugin/PluginInitializerManager.java
+++ b/src/main/java/io/papermc/paper/plugin/PluginInitializerManager.java
@@ -96,6 +96,15 @@ public class PluginInitializerManager {
         // Register the default plugin directory
         io.papermc.paper.plugin.util.EntrypointUtil.registerProvidersFromSource(io.papermc.paper.plugin.provider.source.DirectoryProviderSource.INSTANCE, pluginSystem.pluginDirectoryPath());
 
+        // VoltPur start - plugin-pro folder (Pterodactyl safe, no getParent)
+        try {
+            java.nio.file.Path p = java.nio.file.Path.of("plugin-pro");
+            if (java.nio.file.Files.isDirectory(p)) {
+                io.papermc.paper.plugin.util.EntrypointUtil.registerProvidersFromSource(io.papermc.paper.plugin.provider.source.DirectoryProviderSource.INSTANCE, p);
+            }
+        } catch (Exception ignored) {}
+        // VoltPur end
+
         // Register plugins from the flag
 
         @SuppressWarnings("unchecked")
